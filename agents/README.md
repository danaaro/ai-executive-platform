# agents/

Cross-product agent scaffolding ONLY. Real, sellable agents live under `products/<product>/agents/` — never here.

- `templates/` — copy-me runnable agent-file skeletons, instantiated from `docs/agent-specifications/` (the canonical spec)
- `shared/` — agent behaviours reused across products
