# templates/

Runnable agent-file skeletons (YAML-frontmatter .md). Copy one to start a new agent. Canonical spec: `../../docs/agent-specifications/` — if the two disagree, the spec wins and these get regenerated.
